#include<iostream>
#include<iomanip>
#include<stdio.h>
#include<cmath>

using namespace std;

int main(){
    long long l1 , l2 , r1 , r2 , a , b;
    cin>>l1>>l2>>r1>>r2;
    if(a>=l1 && a<=r1 && a>=l2 )
    if((a>=l1 && a<=r1)&&(b>=l1 && b<=r1)) cout<<min(a , b);
}